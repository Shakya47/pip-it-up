import { describe, it, expect } from 'vitest';
import { createPip } from '../src/index';

describe('createPip', () => {
  it('should be defined', () => {
    expect(createPip).toBeDefined();
  });
});
