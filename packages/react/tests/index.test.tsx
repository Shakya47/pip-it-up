import { describe, it, expect } from 'vitest';
import { PipWrapper } from '../src/index';

describe('PipWrapper', () => {
  it('should be defined', () => {
    expect(PipWrapper).toBeDefined();
  });
});
